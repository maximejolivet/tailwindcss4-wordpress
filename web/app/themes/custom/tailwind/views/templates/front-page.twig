{% extends 'layouts/base.twig' %}

{% block content %}
    <div class="mx-auto max-w-5xl px-4 py-24 text-center">
        <h1 class="text-4xl font-bold tracking-tight">{{ post.title }}</h1>zezeze
        <div class="prose prose-neutral mx-auto mt-6 max-w-none">{{ post.content }}</div>
    </div>
{% endblock %}
